#  Список всех необходимых библиотек для установки через `pip install -r requirements.txt`. (Версии указаны условно, вы можете скорректировать их под свои нужды)

python-telegram-bot==20.3
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.8.5
pyyaml==6.0
pandas==2.1.0
